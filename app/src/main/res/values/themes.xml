<resources>

    <!-- Base application theme. -->
    <style name="Base.App.Theme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="App.Theme" parent="Base.App.Theme"/>

    <style name="App.Theme.Toolbar" parent="Theme.AppCompat">
        <item name="android:textColorPrimary">@color/t_primary</item>
        <item name="android:textColorSecondary">@color/t_secondary</item>
        <item name="actionMenuTextColor">@color/accent</item>
    </style>

    <style name="App.Theme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="App.Theme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Base.App.Theme.Gallery" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="colorPrimary">@android:color/transparent</item>
        <item name="colorPrimaryDark">@android:color/black</item>
        <item name="colorAccent">@color/accent</item>
    </style>

    <style name="App.Theme.Gallery" parent="Base.App.Theme.Gallery"/>

</resources>
